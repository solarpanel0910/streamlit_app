統計表名: 小売物価統計調査(動向編)
データセット名: 主要品目の都市別小売価格ー都道府県庁所在市及び人口15万人以上の市(2000年1月～)
データ取得日: 2025年1月26日
各列の意味:
時間軸（月）: データの対象年月（例：2020年1月）
札幌市、那覇市 など: 各都市におけるレギュラーガソリン1リットルあたりの価格
単位：円
注意点：一部のデータに欠測値が含まれる場合があるため、読み込み時に数値以外のデータをNaNに変換して処理している。


使用した未使用UI部品
st.metric
目的: 単なる数値の表示ではなく、ユーザーが最も知りたい「平均価格」と「前年からの変動（差額・比率）」を直感的に伝えるため。
使用箇所: 画面左側のパネル下部（平均価格と前年比の表示）。

st.container (border=True)
目的: 画面をカード型のエリアに区切り、関連する情報（操作パネルと結果表示）を視覚的にグルーピングして整理するため。
使用箇所: 画面左側の操作パネルおよび数値表示エリア、右側のグラフ表示エリア。

st.info
目的: グラフから読み取れる分析結果やインサイトを、通常のテキストと区別して強調表示するため。
使用箇所: 単年の棒グラフの下（傾向の分析欄）。

st.expander
目的: 元データ（表）は情報量が多く、常に表示されていると画面を圧迫するため、必要な時だけ展開できるようにするため。
使用箇所: アプリ最下部の「生データを見る」セクション。

ギャラリーからの着想
参考にしたアプリ: Stock Peers (Business / Finance カテゴリ)
参考にした点:
画面を左右非対称（1:3）に分割し、左側に操作パネルと重要指標（Metric）、右側にメインのチャートを配置するダッシュボード型のレイアウト。
グラフの上に、現在の値や変動率を大きな数字で表示する構成。
アプリへの反映:
st.columns([1, 3]) を使用して画面分割を行い、左側に selectbox や metric を集約した。
st.container(border=True) を用いて、各要素をカード風のデザインに仕上げ、視認性を高めた。


生成AIの利用について
生成AIの使用: Yes

使用した場面・内容:
エラー解決: データ型変換の際に発生したエラーなどの原因特定と参考修正コードの作成。
UIデザインの提案: Stock Peers のようなレイアウトを実現するための st.columns や st.container の組み合わせ方の実装支援。
コード最適化: 前年比（Delta）を表示するための計算ロジックの実装。
